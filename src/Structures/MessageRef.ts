import { RawMessageReference } from "misc/MessageReferenced";
import { Client } from "../Client";

export class MessageRef {
    constructor(client: Client, messageData: RawMessageReference) {
        
    }
}